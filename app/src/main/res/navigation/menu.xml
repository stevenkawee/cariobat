<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/menu"
    app:startDestination="@id/catalogobat">
    <fragment
        android:id="@+id/catalogobat"
        android:name="com.example.cariobat.catalogobat"
        android:label="fragment_catalogobat"
        tools:layout="@layout/fragment_catalogobat" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.cariobat.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" /><action android:id="@+id/action_global_catalogobat" app:destination="@id/catalogobat"/>
</navigation>